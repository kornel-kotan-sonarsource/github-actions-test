#I didn't find a way to use the environment switch for composite actions
name: 'Deploy Composite Action'
description: 'Composite action for deployment'
inputs:
  environment:
    description: 'Name of environment'
    required: true
runs:
  using: "composite"
  steps:
    - name: Deploy ${{ inputs.environment }}
      run: echo "Deployment to ${{ inputs.environment }}"
      shell: bash
    - name: Log Account ID
      run: echo "Account id  "
      shell: bash